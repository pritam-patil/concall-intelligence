# Plain pip fallback if not using `uv`. Keep in sync with pyproject.toml [project.dependencies].
httpx>=0.27
# nse_fetch.py is ported verbatim from nse-assist, which uses requests, not
# httpx — kept as-is, so this is a second, deliberate HTTP client here.
requests>=2.31
beautifulsoup4>=4.12
# extract.py's PDF parsing (unused pypdf from the initial scaffold dropped in favor of it).
pymupdf>=1.24
supabase>=2.6
pydantic>=2.7
python-dotenv>=1.0
tenacity>=8.2
click>=8.1
